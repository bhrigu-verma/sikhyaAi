<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" fill="none">
  <defs>
    <radialGradient id="bg" cx="38%" cy="28%" r="72%">
      <stop offset="0%"   stop-color="#F7B31E"/>
      <stop offset="55%"  stop-color="#D97706"/>
      <stop offset="100%" stop-color="#92400E"/>
    </radialGradient>
    <radialGradient id="glow" cx="50%" cy="40%" r="55%">
      <stop offset="0%"   stop-color="#FEF3C7" stop-opacity="0.18"/>
      <stop offset="100%" stop-color="#FEF3C7" stop-opacity="0"/>
    </radialGradient>
  </defs>
  <!-- Base shape -->
  <rect width="512" height="512" rx="108" fill="url(#bg)"/>
  <!-- Inner glow -->
  <rect width="512" height="512" rx="108" fill="url(#glow)"/>
  <!-- Subtle top highlight rim -->
  <rect x="1" y="1" width="510" height="220" rx="107" fill="white" fill-opacity="0.06"/>
  <!-- ਸ character -->
  <text
    x="256" y="346"
    font-size="292"
    text-anchor="middle"
    font-family="Georgia, 'Times New Roman', serif"
    font-weight="700"
    fill="white"
    fill-opacity="0.97"
    filter="drop-shadow(0 2px 12px rgba(0,0,0,0.28))"
  >ਸ</text>
</svg>
